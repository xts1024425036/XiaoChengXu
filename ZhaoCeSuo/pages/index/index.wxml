<!--index.wxml-->
<view class="page">
  <!-- 地图 -->
  <map style='wiath:100%;height:100vh'
      scale="17"
      latitude='{{latitude}}'
      longitude='{{longitude}}'
      show-location
      markers="{{markers}}"
      bindmarkertap="direction"
      polyline='{{polyline}}'
  ></map>
</view>
